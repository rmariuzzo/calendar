# Calendar

A simple calendar web view.

## Deployment

Bump the version and tag a release:

```sh
npm version patch   # or minor / major
git push
```

Then, the package will be published to npm automatically by GitHub Actions.
